export const type = `
  type Room {
    id: Int
    name: String
    title: String
    avatar: String
    mode: String
    followerMode: Boolean
    slowMode: Boolean
    collectionCount: Int
    followersCount: Int
    connectionsCount: Int
    waitlistLock: Int
    contentTitle: String
  }
`;
