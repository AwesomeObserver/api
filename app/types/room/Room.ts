export const type = `
  type Room {
    id: Int
    name: String
    title: String
    avatar: String
    mode: String
    followerMode: Boolean
    slowMode: Boolean
    followersCount: Int
    connectionsCount: Int
    usersCount: Int
    guestsCount: Int
  }
`;